<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @season.title %>
</p>

<%= link_to 'Edit', edit_series_season_path(@season) %> |
<%= link_to 'Back', series_seasons_path(@series) %>
